# Premium-Lean Stack for $14.99/month with 70% margins
# Smart templates + minimal dependencies

# Data Science Core (100% free)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

# Single ML Model
xgboost>=2.0.0,<3.0.0

# Explainability (for scientific feel)
shap>=0.42.0,<1.0.0

# Time Series (tiny ARIMA package)
pmdarima>=2.0.0,<3.0.0

# Contextual Bandits (lightweight)
mabwiser>=2.0.0,<3.0.0

# Utilities
tqdm>=4.65.0

# Explicitly REMOVED to save costs:
# tensorflow, keras, transformers, torch (heavy)
# flower (saves $2-5/user/month)  
# prophet (bigger than pmdarima)
# darts (heavy dependencies)
# LightGBM, CatBoost (redundant with XGBoost)
# GPU dependencies
# HuggingFace API costs
